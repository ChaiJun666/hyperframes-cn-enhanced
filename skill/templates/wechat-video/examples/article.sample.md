# AI 视频生产工作流：从文章到短视频

内容团队每天需要把长文章改造成短视频。传统方式需要选题、写脚本、配音、找图、排版和剪辑，周期很长。

新的工作流把文章拆成场景：开场标题、关键观点、数据卡片、案例截图和结尾总结。每一个场景都有一段短旁白，并用统一的视觉模板渲染。

关键数据：一个 12 场景视频可以控制在 90 秒左右。每段旁白先生成音频，再用真实音频时长决定画面停留时间。

结论：稳定的中间产物比一次性生成更重要。保留 `article.md`、`scene-plan.json`、`narration.json` 和 `timeline.json`，失败后才能快速恢复。
