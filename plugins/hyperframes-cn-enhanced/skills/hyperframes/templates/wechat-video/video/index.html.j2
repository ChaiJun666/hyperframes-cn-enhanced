<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>WeChat Article Video</title>
  <link rel="stylesheet" href="styles.css">
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.14.2/dist/gsap.min.js"></script>
</head>
<body>
  <div id="main" data-composition-id="main" data-width="1080" data-height="1920" data-start="0" data-duration="{{TOTAL_DURATION}}">
    {{SCENES}}
  </div>

  {{AUDIO_CLIPS}}

  <script>
    window.__timelines = window.__timelines || {};
    const tl = gsap.timeline({ paused: true });
    gsap.utils.toArray(".scene").forEach((scene) => {
      const start = Number(scene.dataset.start || 0);
      const duration = Number(scene.dataset.duration || 0);
      tl.set(scene, { opacity: 1 }, start);
      tl.fromTo(scene.querySelector(".sc"), { opacity: 0, y: 24 }, { opacity: 1, y: 0, duration: 0.45, ease: "power3.out" }, start + 0.05);
      tl.to(scene, { opacity: 0, duration: 0.15, ease: "power1.in" }, Math.max(start, start + duration - 0.15));
    });
    window.__timelines["main"] = tl;
  </script>
</body>
</html>
